package com.java.instructor.dbpoller.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfiguration {
	/*
	 * @Bean
	 * 
	 * @Primary
	 * 
	 * @ConfigurationProperties("spring.datasource.east") public
	 * DataSourceProperties eastRegionDataSourceProperties() { return new
	 * DataSourceProperties(); }
	 * 
	 * @Bean
	 * 
	 * @ConfigurationProperties("spring.datasource.west") public
	 * DataSourceProperties westRegionDataSourceProperties() { return new
	 * DataSourceProperties(); }
	 */
}
